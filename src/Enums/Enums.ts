export enum FileFormats {
  JSON = 'json',
  YAML = 'yaml',
}

export enum ResultFormat {
  Stylish = 'stylish',
  Plain = 'plain',
  JSON = 'json',
}

export enum TypesOfOperations {
  Add = 'add',
  Delete = 'delete',
  Recursion = 'recursion',
  Changed = 'changed',
  Same = 'same',
}
